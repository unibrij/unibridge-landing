{
  "name": "unibridge-landing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd connect-app && npm ci && npm run build && cd ../fiat-bank-transfer-app && npm ci && npm run build"
  },
  "dependencies": {
    "nodemailer": "6.10.1"
  }
}
