{
  "name": "unibridge-landing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd connect-app && npm install && npm run build && cd ../fiat-bank-transfer-app && npm install && npm run build"
  },
  "dependencies": {
    "nodemailer": "^6.9.0"
  }
}
